
/**
 * Get the docs URL for a given mix slug.
 *
 * @param slug - The slug of the mix.
 * @returns The docs URL.
 */
export function getDocsUrl(slug: string) {
  return `https://github.com/catena-labs/model-mixes/tree/main/mix-definitions/${slug}`
}
